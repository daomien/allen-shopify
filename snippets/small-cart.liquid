{% if cart.item_count > 0 %}
	<div class="small-cart-rows">
	{% for item in cart.items %}
		<div class="small-cart-row grid small-cart-product" id="item-{{ item.variant_id }}">
			<div class="grid-item large--one-quarter medium--one-quarter small--one-quarter small-cart-image">
				<a href="{{ item.product.url | within: collections.all }}">
					<!--<img src="{{ item.product.featured_image.src | product_img_url: 'small' }}" alt="{{ item.title | escape }}">-->
                  	<img src="{{ item | img_url: 'small' }}"  alt="{{ item.title | escape }}" />
				</a>
			</div>
			<div class="grid-item large--three-quarters medium--three-quarters small--three-quarters small-cart-product-info">
				<a class="small-cart-title" href="{{ item.product.url }}">
					{{ item.title }}
				</a><br/>
				<span class="small-cart-price">{{ item.price | money }}<span class="small-cart-quantity"> x {{ item.quantity }}</span></span>
				<a class="small-cart-remove" href="/cart/change/{{ item.variant.id }}?quantity=0" data-id="{{ item.id }}" title="Remove"><i class="fa fa-times-circle"></i></a>
			</div>
		</div>
	{% endfor %}
	</div>
	<h4 style="display: none;" class="small-cart-empty-heading">{{ 'cart.general.empty' | t }}</h4>
	<div class="small-cart-row grid">
		<div class="grid-item large--one-whole text-right">
			<span class="small-cart-total">{{ 'cart.general.subtotal' | t }}</span><span id="cartCost">{{ cart.total_price | money }}</span>
		</div>
		<div class="grid-item large--one-whole small-cart-btns">
			<div class="small-cart-btns-wrapper">
				<a style="display: none;" class="smart-cart-shop-link btn" href="/collections/all">{{ 'cart.general.continue_shopping' | t }}</a>
				<a class="smart-cart-cart-link btn" style="float:left;" href="/cart">{{ 'cart.general.view_cart' | t }}</a>
				<a class="smart-cart-checkout-link btn" style="float:right;" href="/checkout">{{ 'cart.general.checkout' | t }}</a>
			</div>
		</div>
    
	</div>
{% else %}
	<h4 class="small-cart-empty-heading">{{ 'cart.general.empty' | t }}</h4>
	<div class="small-cart-rows"></div>
	<div class="small-cart-row grid">
		<div class="grid-item large--one-whole text-right">
			<span class="small-cart-total">{{ 'cart.general.subtotal' | t }}</span><span id="cartCost">{{ cart.total_price | money }}</span>
		</div>
      {% comment %}
		<div class="grid-item large--one-whole small-cart-btns">
			<div class="small-cart-btns-wrapper">
				<a style="width:100%;" class="smart-cart-shop-link btn" href="/collections/all">{{ 'cart.general.continue_shopping' | t }}</a>
				<a style="display: none;" class="smart-cart-checkout-link btn" href="/checkout">{{ 'cart.general.checkout' | t }}</a>
				<a style="display: none;" class="smart-cart-cart-link btn" href="/cart">{{ 'cart.general.view_cart' | t }}</a>
			</div>
		</div>
    {% endcomment %}
	</div>
{% endif %}